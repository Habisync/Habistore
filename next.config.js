/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove typescript options from here as they belong in tsconfig.json
  reactStrictMode: true,
  // Add other valid Next.js config options here
};

module.exports = nextConfig;
